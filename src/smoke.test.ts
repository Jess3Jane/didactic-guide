import { describe, it, expect } from "vitest";

// Trivial passing test to prove the runner is wired up. Later issues replace /
// supplement this with real unit tests for the simulation core.
describe("scaffolding", () => {
  it("runs the test suite", () => {
    expect(true).toBe(true);
  });
});
